# Notebook article images

Images embedded in Notebook articles via `![alt](/images/notebook/your-image.jpg)`
— either hand-written in an article's `body` field, or through
`/notebook/admin`. Upload the file here through a normal git commit;
the admin tool doesn't upload images itself.
